const fs = require('fs'); const path = require('path'); const ravensPath = path.join(__dirname, 'pages/api/ravens.js'); const promptPath = 'C:\\\\Users\\\\asifa\\\\Downloads\\\\aml prompt.txt'; const oldCode = fs.readFileSync(ravensPath, 'utf8'); const newPrompt = fs.readFileSync(promptPath, 'utf8'); const startIndex = oldCode.indexOf('const SYSTEM_PROMPT = \'); const endIndex = oldCode.indexOf('\;', startIndex) + 2; const replaced = oldCode.substring(0, startIndex) + 'const SYSTEM_PROMPT = \\\n' + newPrompt.replace(/\/g, '\\\\\') + '\\n\;' + oldCode.substring(endIndex); fs.writeFileSync(ravensPath, replaced, 'utf8'); console.log('Successfully updated system prompt in ravens.js');
